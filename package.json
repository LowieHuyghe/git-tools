{
  "name": "git-tools",
  "version": "1.0.0",
  "description": "Tools to make working with Git even easier.",
  "scripts": {
    "start": "npm run compile && node dist/index.js",
    "compile": "rm -rf dist && tsc"
  },
  "dependencies": {
    "colors": "^1.3.2",
    "commander": "^2.18.0",
    "inquirer": "^6.2.0",
    "os-locale": "^3.0.1"
  },
  "devDependencies": {
    "@types/inquirer": "0.0.43",
    "@types/node": "^10.11.4",
    "@types/os-locale": "^2.1.0",
    "tslint": "^5.11.0",
    "tslint-config-standard": "^8.0.1",
    "typescript": "^3.1.1"
  }
}
